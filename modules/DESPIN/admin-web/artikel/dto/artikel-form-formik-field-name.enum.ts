export enum artikelFormFormikFieldNameEnum {
  hashedArtikelId = 'hashedArtikelId',
  tempArtikelId = 'tempArtikelId',
  categorySlug = 'categorySlug',
  title = 'title',
  body = 'body',
  mainImageId = 'mainImageId',
}
